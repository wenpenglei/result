<%= form_for(@amount) do |f| %>
  <% if @amount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amount.errors.count, "error") %> prohibited this amount from being saved:</h2>

      <ul>
      <% @amount.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :one %>
    <%= f.text_field :one %>
  </div>
  <input method="+" type="radio"  name="1" />+<input method="-" type="radio"  name="1"/>-
  <input method="*" type="radio"  name="1" />*<input method="/" type="radio"  name="1"/>/<br>
 
  <div class="field">
    <%= f.label :two %>
    <%= f.text_field :two %>
  </div>
  <div class="field">
    <%= f.label :result %>
    <%= f.text_field :result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
